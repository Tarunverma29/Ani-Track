services:
  app:
    build: .
    ports:
      - "3000:80"
    volumes:
      - ./data:/data
    environment:
      - DATABASE_URL=sqlite+aiosqlite:////data/ani-track.db
      - SECRET_KEY=change-me-in-production
      - ALLOWED_ORIGINS=*
